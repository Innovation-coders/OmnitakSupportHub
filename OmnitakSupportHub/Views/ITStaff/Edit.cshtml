@model OmnitakSupportHub.ViewModels.ITStaffSpecializationViewModel
@{
    ViewData["Title"] = "Edit IT Staff Specialization";
}
<style>
    :root {
        --primary-deep-blue: #0F1419;
        --primary-navy: #1A237E;
        --accent-electric: #00E5FF;
        --accent-gold: #FFB300;
        --surface-card: #242424;
        --text-primary: #FFFFFF;
        --text-secondary: rgba(255,255,255,0.7);
        --gradient-accent: linear-gradient(45deg, #00E5FF, #7B2CBF);
        --shadow-card: 0 8px 32px rgba(0,229,255,0.08);
        --border-glow: rgba(0,229,255,0.3);
    }
    body {
        background: var(--gradient-accent);
        color: var(--text-primary);
        font-family: 'Inter', sans-serif;
    }
    .admin-card {
        max-width: 600px;
        margin: 40px auto;
        background: var(--surface-card);
        border-radius: 24px;
        box-shadow: var(--shadow-card);
        padding: 2.5rem 2rem;
        color: var(--text-primary);
    }
    .form-title {
        font-size: 2rem;
        font-weight: 800;
        background: var(--gradient-accent);
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
        background-clip: text;
        margin-bottom: 1.5rem;
        text-align: center;
    }
    .form-group {
        margin-bottom: 25px;
    }
    .form-label {
        display: block;
        margin-bottom: 8px;
        font-weight: 600;
        color: var(--text-primary);
        font-size: 1rem;
    }
    .form-control, select {
        width: 100%;
        padding: 12px 16px;
        border: 2px solid var(--border-glow);
        border-radius: 8px;
        font-size: 1rem;
        background: var(--surface-card);
        color: var(--text-primary);
        transition: border-color 0.3s;
        box-sizing: border-box;
    }
    .form-control:focus, select:focus {
        outline: none;
        border-color: var(--accent-electric);
        box-shadow: 0 0 0 3px rgba(0,229,255,0.1);
    }
    .invalid-feedback {
        color: #F44336;
        font-size: 0.875rem;
        margin-top: 5px;
        display: block;
    }
    .form-actions {
        display: flex;
        gap: 20px;
        justify-content: center;
        margin-top: 30px;
    }
    .btn-warning {
        background: var(--accent-gold);
        color: var(--primary-navy);
        border-radius: 12px;
        font-weight: 600;
        box-shadow: var(--shadow-card);
        padding: 12px 24px;
        border: none;
        text-decoration: none;
        transition: background 0.2s, color 0.2s, box-shadow 0.2s;
    }
    .btn-warning:hover {
        background: var(--accent-electric);
        color: #fff;
        box-shadow: 0 8px 32px rgba(0,229,255,0.18);
    }
    .btn-secondary {
        background: var(--surface-card);
        color: var(--text-secondary);
        border: 2px solid var(--border-glow);
        border-radius: 12px;
        font-weight: 600;
        box-shadow: var(--shadow-card);
        padding: 12px 24px;
        text-decoration: none;
        transition: background 0.2s, color 0.2s, box-shadow 0.2s;
    }
    .btn-secondary:hover {
        background: var(--accent-electric);
        color: #fff;
    }
</style>
<div class="admin-card">
    <div class="form-title">Edit IT Staff Specialization</div>
    <form asp-action="Edit" method="post">
        <input type="hidden" asp-for="SelectedUserId" />
        <div class="form-group">
            <label class="form-label">User</label>
            <input class="form-control" value="@Model.AvailableUsers.FirstOrDefault(u => u.Value == Model.SelectedUserId.ToString())?.Text" disabled />
        </div>
        <div class="form-group">
            <label asp-for="SelectedSupportTeamId" class="form-label">Change to Existing Team</label>
            <select asp-for="SelectedSupportTeamId" asp-items="Model.AvailableSupportTeams" class="form-control"></select>
        </div>
        <div class="form-group">
            <label asp-for="NewSupportTeamName" class="form-label">Or Create New Team</label>
            <input asp-for="NewSupportTeamName" class="form-control" placeholder="e.g. Endpoint Support" />
        </div>
        <div class="form-actions">
            <button type="submit" class="btn-warning">Save Changes</button>
            <a asp-action="Index" class="btn-secondary">Cancel</a>
        </div>
    </form>
</div>
@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
